<h1>Liste des invités pour : <%= @event.title %></h1>
<p>Nombre de participants : <%= @attendees.count %></p>

<ul>
  <% @attendees.each do |attendee| %>
    <li><%= attendee.first_name %> <%= attendee.last_name %> - <%= attendee.email %></li>
  <% end %>
</ul>
<%# <%= link_to "Modifier l'événement", edit_event_path(@event), class: "btn btn-secondary" %>
<%# <%= link_to "Supprimer l'événement", event_path(@event), method: :delete, data: { confirm: "Êtes-vous sûr ?" }, class: "btn btn-danger" %> 